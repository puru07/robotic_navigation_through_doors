#include "include/astar.hpp"
#include "include/node.hpp"
astar::astar(int startpt,int goal, int w = 1)
{
	int start[] = startpt;
	int goal[]
	int weight = w;
}
astar::getpath(start)
{
	int closelist[];
	std::priority_queue<node> pq;
	node* n0;
	int parent_indx = 0;
	n0 = new node()
	while(~pq.empty())
	{
		n0 = new node();

	}
}